import { BoxStyleDirective } from './box-style.directive';

describe('BoxStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxStyleDirective();
    expect(directive).toBeTruthy();
  });
});
